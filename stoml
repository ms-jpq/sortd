#!/usr/bin/env python3

from sortd.lib import sig_trap
from sortd.s_toml import dump_toml, load_toml


def main() -> None:
    toml = load_toml()
    dump_toml(toml)


try:
    sig_trap()
    main()
except KeyboardInterrupt:
    exit(130)
except BrokenPipeError:
    exit(13)
